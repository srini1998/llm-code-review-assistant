services:
  - type: web
    name: llm-code-review
    env: python
    buildCommand: pip install fastapi uvicorn anthropic
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
